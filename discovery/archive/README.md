# Archived Discovery Snapshots

This directory is intended for local before/after discovery snapshots used during infrastructure validation.

Raw JSON snapshots are ignored by Git because they may contain environment-specific AWS identifiers.
